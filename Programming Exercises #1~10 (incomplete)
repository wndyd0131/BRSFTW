#include <stdio.h>
/* #1
int main() {
	int num, i;
	printf("Input the number: ");
	scanf("%d", &num);
	for (i = 1; i <= 10; i++) {
		printf("%d * %d = %d\n", num, i, num * i);
	}
}
*/

/* #2
int main() {
	int num, i, j;
	printf("Input the number: ");
	scanf("%d", &num);
	for (i = 0; i < num; i++) {
		for (j = 0; j <= i; j++)
			printf("*");
		printf("\n");
	}
}
*/

/* #3
int main() {
	int num, i, j;
	printf("Input the number: ");
	scanf("%d", &num);
	for (i = 0; i < num; i++) {
		for (j = 0; j <= i; j++)
			printf("%d", j + 1);
		printf("\n");

	}
}
*/

/* #4
int main() {
	int num, i, j;
	printf("Input the number: ");
	scanf("%d", &num);
	for (i = 0; i < num; i++) {
		for (j = 0; j <= i; j++)
			printf("%d", i+1);
		printf("\n");
	}
}
*/

/* #5
int main() {
	int num, i, j, k = 1;
	printf("Input the number: ");
	scanf("%d", &num);
	for (i = 0; i < num; i++) {
		for (j = 0; j <= i; j++)
			printf("%d ", k++);
		printf("\n");
	}
}
*/
